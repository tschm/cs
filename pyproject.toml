[tool.poetry]
name = "cs"
version = "0.0.0"
description = "..."
authors = ["Thomas Schmelzer"]
readme = "readme.md"
repository = "https://github.com/tschm/cs"
packages = [{include = "pycta"}]

[tool.poetry.dependencies]
python = ">=3.9.0,<=3.11"
pandas = "*"
plotly = "*"

[tool.poetry.dev-dependencies]
notebook = "*"
jupyterlab = "*"

[build-system]
requires = ["poetry>=1.0.2"]
build-backend = "poetry.masonry.api"
